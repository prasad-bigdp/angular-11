import { PrasadDirective } from './prasad.directive';

describe('PrasadDirective', () => {
  it('should create an instance', () => {
    const directive = new PrasadDirective();
    expect(directive).toBeTruthy();
  });
});
